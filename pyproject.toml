[project]
name = "sdn-ids"
version = "0.1.0"
description = "SDN-IDS - Intrusion Detection System for SDN networks"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests",
    "ryu>=4.34",
    "eventlet==0.30.2",
]

[dependency-groups]
dev = []