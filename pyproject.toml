[project]
name = "sdn-ids"
version = "0.1.0"
description = "SDN-IDS - Intrusion Detection System for SDN networks"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests",
]

[dependency-groups]
dev = []
